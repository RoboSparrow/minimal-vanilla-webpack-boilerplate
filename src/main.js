import 'styles/main.css';// eslint-disable-line import/no-unresolved

document.addEventListener('DOMContentLoaded', () => {

    const node = document.createElement('h1');
    node.textContent = 'hello';
    document.body.appendChild(node);

});
